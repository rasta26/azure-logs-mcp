import type { Client, ClientOptions } from "@azure-rest/core-client";
import type { TokenCredential } from "@azure/core-auth";
export interface MonitorQueryLogsContext extends Client {
    /** The service API version. */
    apiVersion: string;
}
/** Optional parameters for the client. */
export interface LogsQueryClientOptions extends ClientOptions {
    /** The service API version. */
    apiVersion?: string;
    /**
     * The Audience to use for authentication with Microsoft Entra ID. The
     * audience is not considered when using a shared key.
     */
    audience?: string;
}
export declare function createMonitorQueryLogs(credential: TokenCredential, options?: LogsQueryClientOptions): MonitorQueryLogsContext;
//# sourceMappingURL=monitorQueryLogsContext.d.ts.map