{
  "name": "azure-logs-mcp",
  "version": "1.0.0",
  "description": "MCP server for Azure Log Analytics",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@azure/identity": "^4.0.1",
    "@azure/monitor-query-logs": "^1.0.0",
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
